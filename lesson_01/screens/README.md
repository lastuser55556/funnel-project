# Скриншоты задания 1

Сюда положите два файла в формате PNG. Имена должны быть ровно такими:

| Файл | Что должно быть на кадре |
|---|---|
| `01-network.png` | панель Network с фильтром `collect` и списком появившихся строк |
| `02-payload.png` | вкладка Payload одного события: видно `en` и остальные параметры |

Как снять кадр: **Cmd + Shift + 4** на macOS, **Win + Shift + S** на Windows.

Этот файл удалять не нужно.
